[project]
name = "cicd-for-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "atabak rezqi", email = "r.atabak@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
cicd-for-test = "cicd_for_test:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
